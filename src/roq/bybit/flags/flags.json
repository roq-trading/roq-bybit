{
  "name": "Flags",
  "type": "flags",
  "values": [
    {
      "name": "exchange",
      "type": "std::string",
      "required": true,
      "default": "bybit",
      "description": "Exchange identifier (string)"
    },
    {
      "name": "api",
      "type": "std::string",
      "required": true,
      "default": "spot",
      "description": "What api to use (spot, linear, inverse, option)"
    }
  ]
}
